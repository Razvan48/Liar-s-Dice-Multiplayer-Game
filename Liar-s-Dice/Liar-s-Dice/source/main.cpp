#include "Game/Game.h"

int main()
{
	Game::get().start();

	// TODO:


	// de trecut prin cod ulterior ca unele zone au fost scrise in viteza grav de tot

	// interfetele vizuale (avem doar cea de baza, abstracta)
	// client, server (momentan ambele au codul de la lucrarea de licenta)

	// de reparat un bug unde apar << in loc de < la un buton (problema asta e si la licenta) (se reproduce miscand rapid mouse-ul intre buton si afara lui)

	return 0;
}